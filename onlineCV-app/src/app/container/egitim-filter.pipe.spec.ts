import { EgitimFilterPipe } from './egitim-filter.pipe';

describe('EgitimFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EgitimFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
